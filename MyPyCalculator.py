from GUI import app_run
"""
To use this software, you need to have Python 3 installed in your system. 
Then install a virtual environment in the project root directory:
    python -m venv venv

You'll also need to install PySide6 in the virtual environment:
    pip install PySide6
"""

if __name__ == "__main__":
    app_run()
